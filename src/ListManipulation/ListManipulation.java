package ListManipulation;
// Make a class, that has only one attribute called list
// You have to make 5 methods, to manipulate the list items.
// create(string item) -> add a new item to your list
// viewAll() -> print all the list items
// viewOne(int index) -> print the indexed item in the list.
// update(int index, string item) -> update a certain item in the list
// delete(string item) -> delete an item from your list.
// Note: check all the edge cases, like if you have that item in list.
public class ListManipulation {
}
